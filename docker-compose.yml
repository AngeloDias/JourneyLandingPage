version: '1'

services:
  web: nginx:latest
  ports:
    - "8080:80"
  volumes:

  networks:
    - code-network

  php:
    image: php:fpm
    volumes:
      - ./api:/api
    networks:
      - code-network

  app:

networks:
  code-network:
    driver: bridge