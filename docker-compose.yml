version: '1'

services:
  web: nginx:latest
  ports:
    - "8080:80"
  volumes:

  networks:
    - code-network

  php:
    image: php:fpm
    volumes:
      - ./api:/api
    networks:
      - code-network

  app:
    build:
      context: ./
      command: yarn start
    volumes:

    ports:
      - "3000:3000"

networks:
  code-network:
    driver: bridge